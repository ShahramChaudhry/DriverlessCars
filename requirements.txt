# Core ML (local / venv). On Google Colab use requirements-colab.txt instead — pip
# installing torch here replaces Colab's CUDA build with CPU-only (~2 FPS benchmarks).
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.0.0
torch-pruning>=1.3.0

# GUI
gradio>=4.0.0

# Vision
opencv-python>=4.8.0
numpy>=1.24.0
